<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <title>Blog Website</title>
</head>
<body>
    <div class="container">
        <h1 class = "mb-4">Bharat Intern Blog Website</h1>
        <a href="/article/new" class="btn btn-success">Create article</a>

        <% article.forEach(art =>{%>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%=art.title%></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%=new Date(art.createdAt).toLocaleDateString()%>
                    </div>

                    <div class="card-text mb-2">
                        <%=art.description%>
                    </div>

                    <a href="article/<%=art.id%>" class="btn btn-primary">Read More</a>
                    <a href="article/edit/<%=article.id%>" class="btn btn-info">Edit</a>
                    <form action="/article/<%=art.id%>?_method=DELETE" method="POST" class="d-inline"><button type="submit" class="btn btn-danger">Delete</button></form>

                </div>
            </div>
        <%})%>
    </div>
</body>
</html>